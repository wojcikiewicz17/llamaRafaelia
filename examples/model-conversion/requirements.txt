# Low-level comparison utilities now run with the Python standard library only.
# Optional torch/transformers dependencies should be installed separately when
# running the original-model extraction scripts.
